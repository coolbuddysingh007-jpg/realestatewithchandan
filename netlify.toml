[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Optional: redirects for SEO-friendly paths if needed
[[redirects]]
  from = "/market-update"
  to = "/market-update/surrey"
  status = 302
